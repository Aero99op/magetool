services:
  - type: web
    name: magetool-backend
    env: docker
    region: singapore
    plan: free
    dockerContext: .
    dockerfilePath: backend/Dockerfile
    healthCheckPath: /health/live
    envVars:
      - key: PORT
        value: 8000
      - key: PYTHON_VERSION
        value: 3.11.9
